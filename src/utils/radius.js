/**
 * Border-radius scale (design tokens).
 * Use these instead of ad-hoc pixel values so corners stay consistent.
 *
 * sm   — small controls, badges
 * md   — inputs, small cards
 * lg   — content cards (default card radius)
 * xl   — large panels / feature sections
 * pill — fully rounded (buttons, chips, avatars)
 */
export const radius = {
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '24px',
  pill: '999px',
};
